---
topic: Epic 21: Context-Complete, Trip-Aware Planning And Conversion
updated: 2026-08-16T15:33
---

- (direction) Migrate all hardened Epic 21 story guides into the canonical spec-folder plus story-id dispatch model for bmad-build-auto while retaining sprint-status.yaml as the sprint tracker.
- (decision) Use spec folder _bmad-output/specs/spec-epic-21 with dispatch ids 21-1 through 21-16; ids use dashes because stories.yaml accepts only letters, digits, and dashes.
- (decision) Execute stories in dependency order 21-1, 21-2, 21-3, 21-4, 21-5, 21-6, 21-7, 21-8, 21-9, 21-10, 21-13, 21-11, 21-14, 21-15, 21-12, 21-16; numeric ordering does not override dependencies.
- (decision) Keep stories.yaml free of status and retain all Epic 21 entries in sprint-status.yaml as backlog until the orchestrator dispatches and synchronizes each story.
- (decision) Omit optional spec and done checkpoints because the requested unattended flow has no separate checkpoint instruction; real evidence, approval, authorization, environment, and elapsed-time gates remain fail-closed inside the story contracts.
- (capability) CAP-1: Define versioned planning-context profiles and deterministic scope rules; success is readiness evaluated per typed deliverable instance and pinned version without cross-scope leakage.
- (capability) CAP-2: Persist scoped multi-turn clarification state safely; success is monotonic, fenced accumulation that preserves valid answers and rejects stale or foreign state.
- (capability) CAP-3: Run bounded clarification preflight before main answers; success is at most one plan and extraction attempt with blocked turns producing no main-answer side effects.
- (capability) CAP-4: Preserve applied Trip authority across planning modes; success is current-plan, exploration, proposal validation, and unscoped behavior resolved deterministically without pending values becoming applied authority.
- (capability) CAP-5: Preserve canonical Trip paths and supported route coverage; success is durable route authority only from owner-confirmed paths or complete registry projection with safe stale and unsupported behavior.
- (capability) CAP-6: Retrieve and pack evidence by required planning need; success is scope-first candidate selection, fact-level contributions, capacity prioritization, and final-manifest coverage that cannot be hidden by card count.
- (capability) CAP-7: Verify fresh external facts through replayable web scope; success is minimized scoped queries, pinned replay dependencies, and provider failure that preserves uncovered needs.
- (capability) CAP-8: Finalize planning evidence atomically; success is one fenced terminal transaction with no partial or competing answer state.
- (capability) CAP-9: Keep one current chat-to-Trip opportunity available; success is a stable server-owned opportunity refreshed from the latest eligible context and explicitly dismissible.
- (capability) CAP-10: Convert the latest eligible context into a reviewable Trip; success is a separate Trip, primary conversation, and pending proposal with no transcript copy or pre-Apply mutation.
- (capability) CAP-11: Invalidate planning evidence on conversation and Trip deletion; success is synchronous removal of reconstructable owner-derived state while bounded non-reconstructable audit evidence may remain.
- (capability) CAP-12: Establish v6 retrieval qualification infrastructure; success is persisted evaluation, gate-profile, read-policy, and cutover records with controlled runners and no implicit activation.
- (capability) CAP-13: Collect and approve comparable v6 shadow evidence; success is a real evidence window and exact report approved by Feedback or Eval authority and Product Owner without changing production reads.
- (capability) CAP-14: Cut over v6 retrieval through qualified read policy; success is an authorized target-specific compare-and-set transition backed by the approved report and runnable rollback.
- (capability) CAP-15: Retire legacy card-count triggering behaviorally; success is required-need decisions becoming authoritative with COMP-01 through COMP-06 closure and approved retirement evidence.
- (capability) CAP-16: Physically remove expired legacy compatibility; success is cleanup only after the rollback window, passing cleanup report, no incident, approval, and a qualified v6 rollback target.
- (constraint) Planning readiness and evidence decisions must pin all relevant profile, policy, comparator, schema, content, Trip, proposal, route, retrieval, parser, prompt, model, and evaluator identities or record explicit absence.
- (constraint) Use existing modular-monolith ownership, PostgreSQL and Drizzle, NestJS admission, Worker runtime, AI Gateway purposes, and owner ports; add no parallel endpoint, service, queue, cache, feature flag, model purpose, or environment authority.
- (constraint) Only applied Trip state and owner-confirmed canonical paths are current authority; prose, confidence, ids, similarity, model output, pending proposals, and card counts cannot manufacture authority.
- (constraint) Prepared and terminal AI Ask state share one command fence and owner transaction boundaries; stale, retried, deleted, or failed work cannot become visible.
- (constraint) Use pnpm test:unit for infrastructure-free checks and serial pnpm test:integration for PostgreSQL-backed checks with local resetTestDatabase ownership; baseline checks are lint, typecheck, and build.
- (constraint) External evidence, human approvals, actor authorization, target identity, and rollback-window expiry are genuine fail-closed gates and cannot be synthesized by local success.
- (note) The full per-story Given-When-Then criteria, exact paths, commands, dependency checks, Block If conditions, and source references are preserved in story-contracts.md.
- (note) Wrapper-only legacy content dropped during migration: backlog status lines and empty Dev Agent Record placeholders; execution status belongs to generated story specs and sprint-status.yaml.
- (event) Coherence validation passed: all 16 capabilities have intent and testable success, constraints bend implementation, non-goals are explicit, and the success signal is demonstrable.
- (event) Preservation validation passed provisionally: every load-bearing legacy guide claim is routed to SPEC.md, story-contracts.md, or an adopted companion; final file-level comparison remains required.
- (event) Story breakdown schema decision: 16 unique prefix-safe quoted ids in dependency order, with no status fields and descriptions pointing to CAP ids plus story-contracts.md.
- (event) Final preservation validation passed: 16 story sections, 60 Given blocks, 106 actionable checklist tasks, and 93 source references match the committed hardened guides; only backlog wrappers, empty agent records, deprecated skill naming, and migrated artifact paths changed.
- (event) stories.yaml validation passed: 16 quoted unique prefix-safe ids in dependency order, no status fields, all seven SPEC companions resolve, and dispatch id 21-1 has zero existing story-spec matches with SPEC.md present.
- (event) Sprint integration validation passed for Epic 21: epic-21 and all 16 mapped story keys remain backlog and folder dispatch is ready; project-wide sprint validator still reports three pre-existing illegal superseded-by statuses in Epics 3 and 12 that this migration did not reinterpret.
- (decision) Migration 0066 is already owned by discovery_candidate_jobs; supersede the initial Epic 21 numbering so Story 21.1 uses 0067, Story 21.2 uses 0068, and Story 21.5 uses 0069, preserving a collision-free explicit sequence.
- (event) Coherence validation passed after migration renumbering: explicit Epic 21 migrations are collision-free and remain sequential at 0067, 0068, and 0069.
- (event) Preservation validation passed after migration renumbering: only the three explicit migration filenames changed; story intent, acceptance criteria, tasks, dependencies, and dispatch inventory remain unchanged.
- (direction by user) User approved a clean-break rewrite of Epic 21 because there are no production users and existing data may be deleted; preserve traveler outcomes while removing rollout overengineering.
- (decision by user) Retire CAP-12 through CAP-16 and their shadow, qualification-profile, read-policy, cutover, compatibility-retirement, and cleanup control planes; retired capability IDs are never reused.
- (capability by user) CAP-17: Activate required-need retrieval in one clean break; success is one active path with card-count authority removed and recovery by guarded reset/migrate/seed before durable data exists.
- (constraint by user) Epic 21 may add at most one table, planning_context_sessions, and one forward migration, 0073_clean_break_trip_aware_planning.sql; reuse existing assistant_retrieval_decisions and existing Chat, Trip, Usage, provenance, feedback, and audit storage.
- (constraint by user) Clean break is allowed only for explicitly disposable targets; if durable user data or a non-disposable target is discovered, stop and require expand-migrate-contract design.
- (decision by user) Replace the sixteen-story plan with seven vertical stories: data shape; clarification and mode; route and required needs; web and atomic answers; chat-to-Trip conversion; deletion; direct activation and count removal.
- (event by user) Product Owner approval is represented by the user's explicit 2026-08-16 instruction to simplify Epic 21 and delete old data.
- (event) Coherence validation passed after clean-break re-derivation: CAP-1 through CAP-11 and CAP-17 each have intent and testable success; CAP-12 through CAP-16 remain explicitly retired; constraints enforce one table, migration 0073, seven-story order, and disposable-target stop behavior.
- (event) Preservation validation passed: traveler outcomes for clarification, planning modes, canonical routes, required needs, scoped web, atomic answers, conversion, and deletion remain mapped; only pre-launch rollout machinery and wrapper ceremony were dropped.
- (event) stories.yaml validation passed: seven quoted unique prefix-free ids 21-1 through 21-7, no status fields, exact story-contract sections, no generated story specs, and sprint keys match all titles.
- (event) Sprint tracker validation passed after normalizing three pre-existing superseded terminal statuses to supported done values with explanatory comments; Epic 21 and its seven stories remain backlog.
- (event) Final artifact validation passed: migration inventory ends at 0072, every active Epic 21 initial migration reference is 0073_clean_break_trip_aware_planning.sql, and git diff whitespace validation passes.
- (direction by user) User approved the review recommendation to replace the seven-story clean-break draft with eight smaller independently implementable stories.
- (decision by user) Story 21.1 owns only the minimal planning-session schema and flat bounded session contract; later stories own their own code constants, and existing reset/seed guards are inspected rather than changed without evidence.
- (decision by user) Split clarification from planning-mode authority: Story 21.2 owns multi-turn clarification only; Story 21.3 owns deterministic mode and applied Trip authority only.
- (decision by user) Split route authority from retrieval: Story 21.4 owns a static code-reviewed route manifest and owner-confirmed path references; Story 21.5 owns required-need retrieval and removes card count in the same change.
- (decision by user) Story 21.6 reuses the exact existing web-search and AI Ask finalization modules without a new run schema or finalization abstraction.
- (decision by user) Story 21.7 keeps chat-to-Trip lifecycle minimal and reuses recommendation/idempotency/proposal commands; no generic opportunity workflow engine is allowed.
- (decision by user) Story 21.8 uses foreign-key cascades and existing delete commands first, adds explicit cleanup only for proven gaps, and performs final clean-break verification with scoped rg rather than a custom scanner or automatic database reset.
- (constraint by user) Migration 0073 is owned and finalized by Story 21.1; no later story may amend an already-applied migration or add another Epic 21 migration.
- (constraint by user) Each story has three to five implementation tasks, no new Worker/service/package/environment mode/control plane, and no generic graph, workflow, evaluation, or release framework.
- (decision) Keep existing count columns as inert telemetry; remove only count-based runtime/config/test authority so Story 21.1 can finalize migration 0073 without coupling schema foundation to Story 21.5 behavior.
- (decision by user) Replace the old clarification graph/claims/attempt contracts with one flat bounded planning-session payload and one deterministic reducer; reduce canonical clarification fixtures to eight traveler-visible cases.
- (decision by user) Replace the opportunity/manifest lifecycle with the existing recommendation aggregate and four states; reduce conversion fixtures to eight cases and keep existing idempotency/owner fences.
- (decision by user) Replace route publication and normalized retrieval/web run contracts with one static route constant, one pure required-need evaluator, existing web-search storage, and one bounded existing retrieval snapshot.
- (decision by user) Keep existing count columns as inert telemetry so stories remain independently buildable; remove the decision branch, configuration, tests, and runbook authority instead of adding schema-cleanup coupling.
- (event) Coherence validation passed for the eight-story breakdown: every story has one primary decision, four or five tasks, exact owner paths, and no generic graph/workflow/run/release framework.
- (event) Preservation validation passed after companion simplification: clarification, planning authority, route safety, required needs, web provenance, conversion, and deletion outcomes remain; only implementation machinery without current user value was retired.
- (event) stories.yaml validation passed with eight quoted unique prefix-free ids, matching eight active Epic headings and eight backlog sprint keys; no generated story spec pins an old id.
- (event) Companion validation passed: canonical contracts use one flat session, four modes, six route results, four need outcomes, four conversion states, 52 focused fixture IDs, one migration 0073, and no old graph/attempt/manifest/registry-publisher symbols.
- (event) Sprint status validator and git diff whitespace validation pass after the eight-story rebaseline.
