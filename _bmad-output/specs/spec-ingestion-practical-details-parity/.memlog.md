---
topic: Ingestion pipeline practical_details/tags parity with legacy extraction
goal: Canonical ingestion cards carry the practical-details buckets, ordered stops, and tags that downstream retrieval/search/UX already consume
updated: 2026-07-29T11:04
---

- (note) Gap 1 confirmed: ordered_stops exists only in legacy extraction - prompt rule prompts.ts:69, parser normalizeOrderedStop extraction.ts:484-508; ingestion Candidate type, parseCandidate, knowledge_ingestion_candidates schema, and all four card-insert paths lack it; consumers retrieval/approved-knowledge.ts:11 and search.ts:548-560 still expect it
- (note) Gap 2 confirmed: practical_details buckets (tips, warnings, cost_notes, parking_notes, kid_notes) parsed by extraction (up to 20 entries) but ingestion never sets practicalDetails, default {}; retrieval allowlist and search text consume exactly these keys plus ordered_stops
- (note) Gap 3 confirmed: tags parsed by extraction (max 12, normalized, deduped) but ingestion never sets tags, default []; tags feed card searchable text
- (note) Gap 4 excluded: extraction clampConfidence honors official/partner/curated by source metadata while ingestion hardcodes community/unverified; judged intentional canonical conservatism, needs separate decision
- (note) Gap 5 excluded: extraction inferLocationFallback rescues scope-less drafts for known Central-VN corridors while ingestion rejects scope-less candidates as candidate_missing_required_fields; judged intentional evidence-grounding strictness
- (direction by user) Scope = full practical_details parity: gaps 1-3 (ordered_stops, five buckets, tags) in scope; gaps 4-5 (confidence ceiling, location fallback) out of scope
- (decision) Target protocol v2 multi-fact discovery only; v1 single-fact path stays untouched because every new job is created with protocol_version=2 and v1 remains only for legacy compatibility
- (capability) CAP-1 intent: v2 discovery extraction contract lets the model attach practical-details buckets (tips, warnings, cost_notes, parking_notes, kid_notes) and tags to a candidate when the source supports them; success: a capture describing a venue with parking/kid/cost detail yields candidates whose buckets and tags survive parse, candidate row, and card row in DATABASE_URL_TEST-backed tests
- (capability) CAP-2 intent: a capture principally describing an ordered itinerary, route, or stop list yields exactly one route_note candidate carrying the complete ordered stop sequence in source order including intentional repeats, never one candidate per stop; success: a multi-stop itinerary capture produces one route_note candidate whose ordered_stops equals the source sequence with no per-stop sibling candidates
- (capability) CAP-3 intent: ingestion normalizes and validates practical details, ordered stops, and tags with legacy-equivalent safety rules and persists them into knowledge_cards on every terminal publish path; success: adversarial outputs (numbered, annotated, prose, contact-laden, or over-limit stops, buckets, tags) are rejected or normalized exactly per legacy rules, and published, verify_first, and review_recommended cards carry the values in DB-backed tests
- (constraint) Canonical invariants unchanged: evidence grounding, independent judgment, relation judgment, fencing, and stage lifecycle keep their shape; buckets, stops, and tags annotate a candidate and never become standalone candidates
- (constraint) Schema change is an additive, backward-compatible migration on knowledge_ingestion_candidates (practical_details default {}, tags default []) that is safe for in-flight rows
- (constraint) Legacy-equivalent ceilings: ordered_stops at most 40 labels after stripping numbering and annotations, practical_details keys bounded, tags at most 12 normalized and deduped; sensitive-pattern rejection and raw-overlap guards preserved
- (constraint) Ungrounded candidates remain suppressed regardless of attached buckets or tags; judgment thresholds and grounding rules are untouched
- (note) Non-goal: confidence ceiling by source metadata (gap 4)
- (note) Non-goal: location-fallback heuristics for scope-less candidates (gap 5)
- (note) Non-goal: backfilling practical_details or tags onto existing ingestion-created cards; operator rerun via rerunKnowledgeIngestionJob is the mechanism
- (note) Non-goal: retrieval, search, or UX consumer changes; the allowlist already covers the six keys
- (note) Non-goal: legacy extraction worker deprecation or removal
- (assumption) Assumed gaps 4-5 exclusions are intentional canonical design (conservative confidence, strict scope grounding) per user scope decision; reviving either requires its own spec
- (assumption) Assumed existing ingestion-created cards keep empty practical_details and tags; no card-data migration accompanies the candidate-schema migration
- (question) Open: on attach/conflict relation outcomes, do incoming buckets and tags merge into the target card practical_details and tags, or does attach stay evidence-only with target card content untouched?
- (question) Open: copy legacy per-key ceilings verbatim for buckets and tags (20 keys max, 500-char strings, 10-item arrays, 40-char tags) or tighten them for the canonical pipeline?
- (decision) Spec-authored companion contract-gaps.md holds the file-and-line gap inventory plus consumer expectations so the kernel stays lean
- (note) Success signal: a supervised ingestion worker run over an ordered-itinerary capture (for example a 20-stop Hanoi-to-HCMC post) produces one route_note card whose practical_details.ordered_stops preserves source order, and AI-answer plus search context for that route renders the stop list; demonstrated by a DATABASE_URL_TEST pipeline test and a local worker run
- (event) Self-validate pass 1 (coherence): PASS - CAP-1..3 each carry intent+success, intents stay WHAT-level, 5 constraints bend design, 5 explicit non-goals, success signal is testable, IDs stable; prose lean
- (event) Self-validate pass 2 (preservation): PASS - all load-bearing analysis claims landed in SPEC.md or contract-gaps.md (gaps 1-3 with file lines, gaps 4-5 excluded with rationale, consumer expectations, v2-only decision, user scope direction, 2 open questions); wrapper-only drop: conversational analysis process metadata
- (event) Spec finalized: SPEC-ingestion-practical-details-parity, 3 capabilities, 1 spec-authored companion (contract-gaps.md), 2 assumptions, 2 open questions
- (question by user) Does the empty-details problem affect only pre-fix data or also new data after the fix?
- (note) Answer: post-fix candidates and cards carry details, so the gap closes for new data; pre-fix cards stay empty, and rerunKnowledgeIngestionJob does not enrich them because relation attach is evidence-only today (persistV2CandidateCard attach path adds evidence without touching card content)
- (note) Consequence: the attach-merge open question doubles as the old-data remediation lever - merge-on-attach lets operator rerun enrich pre-fix cards; evidence-only leaves them empty until manual operator action
- (decision by user) Ceilings resolved: copy legacy per-key limits verbatim for now - practical_details 20 keys max, 500-char strings, 10-item arrays; tags 12 max at 40 chars each; ordered_stops 40 max labels
- (event) Spec re-derived: ceilings constraint now explicit verbatim copy, ceilings open question retired, attach-merge open question retained with old-data remediation context
- (question by user) With no pre-fix data to remediate (development resets the database frequently), which attach-merge option should be chosen?
- (note) Analysis: without old-data remediation needs, evidence-only attach freezes card content at first publication - corroborating sources would enrich evidence but never the card, defeating the attach/promote mechanism; merge-on-attach adds no new risk class because the pipeline already auto-publishes judged AI content, and merged values pass identical normalization, ceilings, grounding, and judgment
- (decision) OQ1 resolved: attach relation outcomes merge incoming buckets, tags, and ordered_stops into the target card practical_details and tags as a deduped union within ceilings, with content-version bump and re-index; conflict outcomes remain evidence-only with suppression
- (event) Spec re-derived: CAP-3 extended with attach-merge persistence, pre-fix-data assumption updated to reflect frequent development resets, open questions section now empty and removed
