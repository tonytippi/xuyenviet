---
scope: XuyenViet AI Travel Information MVP
purpose: architecture spine for epics and stories
altitude: project MVP
updated: 2026-08-12T11:02
---

- (constraint) Input PRD finalized at _bmad-output/planning-artifacts/prds/prd-xuyenviet-2026-07-04/prd.md; architecture must satisfy AI Ask, Google Login, memory, approved knowledge cards, web search fallback, source/confidence display, private beta access, and admin knowledge approval.
- (constraint) Repo is greenfield for application code; only README, BMad artifacts, and opencode config exist.
- (version) Next.js docs checked 2026-07-04; latest shown 16.2.10, App Router supports full-stack React with Server Components and route handlers.
- (version) Auth.js OAuth docs checked 2026-07-04; Google provider and PostgreSQL/Drizzle adapters are available; Auth.js project now part of Better Auth but docs still fit MVP Google OAuth.
- (version) pgvector repo checked 2026-07-04; current instructions show v0.8.4, Postgres 13+ support, HNSW/IVFFlat, hybrid search with Postgres full-text search.
- (version) Tavily Search API docs checked 2026-07-04; /search returns title, url, content/snippets, score, country=vietnam option, include/exclude domains, time_range.
- (version) OpenAI Retrieval docs checked 2026-07-04; vector stores exist, but MVP chooses DB-owned retrieval for approval, provenance, and deletion control.
- (version) Drizzle docs checked 2026-07-04; lightweight TypeScript ORM supports PostgreSQL, migrations, schema definition, and serverless-ready access.
- (decision) Adopt modular monolith on Next.js App Router for MVP; binds UI, API routes, admin, and AI orchestration into one deployable; prevents chat/admin/retrieval features from choosing incompatible service boundaries.
- (decision) Use PostgreSQL as system of record with pgvector for approved knowledge embeddings and memory embeddings; binds retrieval, provenance, deletion, and approval state to one owned data plane; prevents provider-hosted vector stores from becoming hidden source-of-truth.
- (decision) Use Drizzle for schema, migrations, and typed DB access; binds database evolution through code-reviewed migrations; prevents ad hoc SQL/schema drift across chat, admin, and eval work.
- (decision) Use Auth.js Google OAuth plus email allowlist and role table for private beta/admin; binds access decisions server-side; prevents client-only gating or separate admin auth.
- (decision) Model core domains as Auth, Chat, Memory, Knowledge, Retrieval, Search, AI Orchestration, Admin, Feedback/Eval, and Audit; dependencies flow inward through server modules, not direct cross-feature UI/database access; prevents circular ownership of user memory, cards, sources, and answer provenance.
- (decision) All mutations of memory, knowledge cards, source records, approvals, feedback, and deletion requests go through server actions/route handlers with authenticated user and audit context; prevents client-side writes and unaudited operator changes.
- (decision) Knowledge card lifecycle is draft -> approved -> archived; only approved cards can be retrieved for user answers; source material remains operator-only; prevents raw/unreviewed/Facebook-derived content from leaking into normal AI grounding.
- (decision) Retrieval pipeline order is memory + current trip context, approved knowledge hybrid retrieval, web search fallback, then general model reasoning; binds PRD priority into one orchestrator; prevents feature teams from bypassing source/confidence rules.
- (decision) Web search fallback uses a search adapter interface with Tavily as MVP seed provider; adapter output must normalize title, url, snippet/content, score, checkedAt, sourceType=external, confidence=unverified; prevents provider lock-in and missing provenance.
- (decision) OpenAI is used through a provider adapter for chat, extraction, embeddings, and evaluation prompts; all prompts must pass explicit source bundles and require structured provenance output; prevents model calls from inventing source labels or writing memory directly.
- (decision) Answer provenance is first-class data: every assistant response stores categories used, knowledge card IDs, memory IDs, web result IDs, model name, and prompt/eval metadata; prevents UI-only citations that cannot be audited.
- (decision) Memory is split into long-term user_memory and conversation trip_context; extraction proposes candidate memory updates and the memory module validates allowed fields before persistence; prevents sensitive data capture and current-trip facts polluting persistent profile.
- (decision) Deletion requests create a tracked privacy request; fulfillment deletes profile memory and derived memory embeddings and records tombstones; conversation transcript deletion remains admin-mediated during beta; prevents partial silent deletion and re-embedding deleted facts.
- (decision) Private beta operations run in dev, staging, and production environments with separate databases and secrets; production access requires allowlisted Google account and role check; prevents test data, beta users, and admin rights from mixing.
- (decision) Use hosted serverless-friendly Postgres with pgvector and a serverless Next.js deployment seed; operational owner must confirm provider before implementation; prevents architecture from relying on unmanaged local infrastructure.
- (decision) Streaming chat response is allowed only after retrieval/search context and provenance ledger are assembled; long extraction/embedding jobs may run as background tasks with status; prevents partial answers that cannot show required source/confidence section.
- (assumption) Seed app stack is create-next-app TypeScript with App Router, Tailwind-compatible styling, Drizzle, PostgreSQL, Auth.js, OpenAI SDK, Tavily adapter, and pgvector-capable hosted Postgres.
- (assumption) Latency target for beta: start streaming visible answer within 5 seconds when no web search is needed and within 10 seconds when web search is needed; final target must be validated in implementation.
- (question) Confirm final deployment provider and hosted Postgres provider before implementation.
- (question) Confirm OpenAI data-processing/privacy wording and whether Zero Data Retention or equivalent account setting is available.
- (question) Confirm Facebook content reuse policy before operators store copied post text beyond short provenance summaries.
- (decision) Reviewer gate fixes applied: canonical source/provenance schema, media ingestion, provider privacy gate, Tavily validation spike, eval contract, aggregate command ownership, confidence semantics, deletion propagation, embedding lifecycle, and retrieval decision record.
- (event) spine finalized
- (decision) Course correction: removed email allowlist from active architecture; public traveler access allowed, Google OAuth required for persisted personalization, admin/operator access controlled by server-side roles.
- (decision) Refined auth architecture: public entry/sign-in routes only; AI Ask requires authenticated session; admin/operator requires session plus role.
- (decision) Simplified architecture: Chat/Trips owns conversations, trip projects, context, embeddings, and user-owned chat/trip deletion; removed dedicated privacy_requests and long-term user_memory workflow from MVP.
- (decision) Epic 5 MVP retrieval starts with deterministic metadata-filtered approved-card retrieval, not Postgres full-text search or vector ranking; binds Story 5.1 to searchable card metadata and reviewed summaries; prevents Story 5.1 from requiring embedding/backfill/provider-ranking infrastructure before retrieval eligibility and provenance are proven.
- (decision) Hybrid retrieval remains the target enhancement after metadata retrieval is observed: Postgres full-text and pgvector can be introduced behind the Retrieval module only after metadata eligibility, source-bundle shape, backfill lifecycle, and provenance tests are stable; prevents early index choice from leaking into orchestration or UI contracts.
- (decision) Epic 5 source bundles are assembled from traveler-safe snapshots only: selected trip context, chat context, approved knowledge summaries/source metadata, web snippets, and general marker; raw source material and operator-only fields are excluded from traveler AI Ask prompts and provenance snapshots.
- (decision) Retrieval eligibility is fail-closed: unknown, missing, draft, archived, rejected, stale, disabled, or operator-only state excludes an item from traveler retrieval; tests must cover exclusions before Story 5.1 is considered done.
- (decision) Frontend redesign architecture update: adopt three canonical traveler UI states for MVP: public logged-out homepage at root, logged-in empty AI Ask state with left sidebar and centered composer but no right panel, and active AI Ask workspace with left sidebar, center answer, and right contextual detail panel for selected answer entities; binds frontend stories to one route/shell model and prevents divergent homepage/chat/detail implementations.
- (decision) Frontend route ownership: keep root route as public logged-out/entry surface, keep /ai-ask as the authenticated AI planning shell, and represent active conversation/trip selection with query parameters or nested route segments owned by Chat/Trips; binds routing to existing App Router structure and prevents introducing a separate frontend app or cross-feature shell owner.
- (decision) Contextual detail panel state: the right panel is UI state derived from persisted assistant message content and persisted provenance/retrieval rows; it must not become a separate mutable aggregate or source of truth; selected entity references must resolve through Chat/Trips, Retrieval, Knowledge, Search, or provenance query helpers according to owning module.
- (decision) Answer entity contract: selectable answer entities are stable render descriptors derived at answer-render time from assistant message structure plus provenance/source-bundle snapshots; descriptors may include type, label, display text, source category, and owning IDs, but UI must not parse free-form answer text to create provenance or source claims.
- (decision) Sidebar data loading: conversation history and trip projects remain Chat/Trips-owned read models loaded server-side for the shell; normal travelers only receive their own conversations/projects and authorized admin links are role-gated server-side; prevents client-side ownership filtering and duplicated sidebar data owners.
- (constraint) Updated UX spines at _bmad-output/planning-artifacts/ux-designs/ux-xuyenviet-2026-07-05/DESIGN.md and EXPERIENCE.md are binding for the traveler shell: Inter-led white/stone edge-to-edge workspace, flat grouped sidebar, conditional inspector, icon-first low-chrome composer, and responsive sheet behavior.
- (decision) Frontend visual foundation: src/app/layout.tsx and src/app/globals.css own app-wide font loading, CSS design tokens, base surfaces, and reduced-motion behavior. Data-free reusable UI primitives live under src/components/ui; feature components consume them and retain domain behavior. This prevents each feature from redefining shell colors, typography, focus styles, and component variants.
- (decision) Icon contract: use one locally owned typed SVG icon module under src/components/ui for all product icons. Icon-only controls require an accessible name, visible focus state, and hover/focus tooltip. Do not add a second icon library or inline unrelated SVG sets in feature components without an architecture update; destructive confirmations retain explicit text.
- (decision) AI Ask shell composition: src/app/ai-ask/page.tsx remains the server-authenticated route composer; AiAskComposer owns transient client workspace state including composer draft, mobile sheet state, streaming state, and selected detail descriptor. Conversation/trip selection remains URL-owned Chat/Trips state. Desktop, tablet, and mobile render the same server-loaded shell data and client state, with CSS layout and sheets changing presentation only; they must not introduce breakpoint-specific loaders or parallel state owners.
- (decision) AI Ask route reconciliation: URL-selected conversation/project state is canonical after create, select, delete, project switch, and stream completion. AiAskComposer may optimistically update transient messages/list summaries while a request is in flight, but must reconcile via router navigation or refresh after terminal mutation; stale/deleted/not-owned route resources clear client selection and load the server-safe shell. The implementation chooses push versus replace per navigation semantics but may not leave a created active conversation only in local state.
- (decision) Responsive selected-detail contract: a selected persisted descriptor requires exactly one interactive detail surface. Desktop column and mobile sheet are controlled presentations of the same selected descriptor; inactive duplicate surfaces are inert and excluded from assistive technology. A breakpoint change preserves selection, route state, and predictable focus behavior; it does not create an independent selected-detail state.
- (decision) Brownfield corrections: traveler knowledge retrieval currently uses active indexed lexical search documents followed by approved/source-safe eligibility checks, with an indexing worker. Production deployment requires separately supervised worker processes for extraction and indexing in addition to request-serving Next.js. Persisted source enums are source_type curated|community and verification_status unverified|verified; richer display confidence remains derived. Current answer annotations support source|warning|trip_fact|action and are best-effort post-answer gateway enrichment.
- (decision) Corrected descriptor source-category contract to trip_context|chat_context|knowledge|web|general, matching persisted provenance. Implemented and verified URL reconciliation for both successful and persisted answer-failed terminal stream paths so newly selected conversations retain conversationId and active tripProjectId across refresh.
- (event) spine finalized
- (decision) AI-first update deliverables are an architecture spine contract for consistent engineering implementation plus a detailed solution design for engineers and operators.
- (decision) Knowledge card is the canonical aggregate only after deterministic validation and independent AI judging. Extracted candidate claims remain immutable job/evaluation artifacts; no separate claim aggregate exists in the MVP. Evidence, evaluations, relations, and review recommendations depend on the card.
- (decision) Knowledge state mutations use PostgreSQL as source of truth with a transactional outbox/dirty index marker. Knowledge owns all state transitions; each transition updates card state, audit, and index state atomically. Retrieval fail-closed re-checks current owner-row eligibility before source-bundle use. High-risk conflict/source removal immediately downgrades or suppresses and disables the projection; indexing is asynchronous, idempotent by card ID and content version.
- (decision) Knowledge audit is intentionally lean at scale: knowledge cards retain current state, monotonic content version, current judge summary, and currently effective evidence only. Durable audit records only meaningful state transitions and operator actions. Extraction/evaluation artifacts are operational data with short retention, not an immutable fact-version graph.
- (decision) Evidence is retained separately from active use. New evidence supplements current evidence by default; it replaces/deactivates older evidence only for time-varying facts or when newer evidence makes old evidence unsuitable. Retrieval uses only active evidence, capped at three supporting and one conflicting record per card, selected by recency, source independence, and quality. Inactive evidence has short operational retention with a deactivation reason.
- (decision) Candidate relation boundary: scope candidate matching by knowledge type plus normalized location/route; only auto-attach when code validates scope/evidence and an independent judge confirms same fact with equivalent conditions. Create a new card for materially distinct compatible facts; suppress redundant/same-source candidates; recommend review for ambiguous relations, state-changing merge, conflict, high-risk topic, or missing observed date.
- (decision) Ingestion uses one stateful orchestration job per source capture version with stages queued, triaging, extracting, judging, relating, and terminal published/suppressed/review-recommended/verify-first/failed outcomes. A failed stage retries without re-running completed stages. Indexing remains a separate projection worker because it also responds to operator edits, source removal, and conflict transitions.
- (decision) Verification is a separate dimension, not a publication state: verification_state is not_required, required, corroborated, or failed. Required claims may be active only as conditional caveats and cannot drive itinerary decisions; corroboration does not automatically imply confirmed; failed verification suppresses or downgrades by risk.
- (decision) Automation uses a dedicated service actor, system-knowledge-pipeline. Source submitter remains provenance only; automated triage, judge, relation, publication, conflict downgrade, and indexing mutations are not attributed to the submitter. Audits link ingestion job, source, model/prompt version, and submitter where applicable.
- (decision) Review-gate corrections: source capture artifacts must be immutable/versioned and evidence/jobs reference an exact content hash; ingestion stages require transactional claims, leases/fencing, idempotency, and compare-and-swap versions; source removal needs an atomic retryable evidence-to-card cascade; sampling, retention, community-pattern independence, freshness routing, review-version binding, and a machine-readable retrieval use matrix are mandatory workflow invariants.
- (decision) Final review corrections: persist an evidence independence key and require distinct keys for community patterns; delete a Facebook source/capture artifact and dependent operational artifacts after 180 days when it supports no active or reviewable card; web-search failure or low-confidence fallback must produce an explicit unable-to-verify recommendation rather than unsupported guidance.
- (event) AI-first community knowledge architecture update finalized
- (constraint) The updated PRD adds the Trip Planning Foundation: structured owner-scoped plan state, one primary conversation, basic Trip Home, and user-confirmed change proposals; dynamic providers and collaboration remain outside this tranche.
- (decision) Chat/Trips remains the sole owner of the Trip Project aggregate and its structured plan; itinerary/anchors/activity state must not be represented as mutable chat context or answer annotations.
- (decision) AI creates version-fenced Trip Change Proposals only; an owner-scoped server command validates proposal scope, expiry, and affected item versions before atomically applying state and audit history.
- (decision) Each Trip Project receives one primary conversation through an idempotent owner-scoped migration that preserves historic linked conversations and never promotes them into structured state without a user-confirmed command.
- (decision) Trip Home is a derived deterministic Chat/Trips read model: unresolved proposal or confirmed-item gap first, then next planned/confirmed leg, then preparation; no provider-dependent inference and no error for an open idea item.
- (change) Aligned AD-10 YouTube wording with the canonical AI-first publication policy: evidence generation remains unverified/community-labeled as applicable, while AD-7 risk, review, verification, conflict, and retrieval gates determine use.
- (event) Trip Planning Foundation architecture update finalized
- (decision) Trip Planning repair: one versioned owner-scoped trip_project_constraints record is the canonical stable constraint state; chat extraction only proposes changes, and budget range is retained while budget tracking/actual expenses remain deferred.
- (decision) Trip Planning repair: plan items use discriminated anchor roles, same-project acyclic activity-to-leg parentage, scoped unique ordering, and explicit backup targets/decision groups; commands atomically protect affected order scopes.
- (decision) Trip Planning repair: proposals fence the aggregate plus affected items and structural preconditions; an idempotent Chat/Trips expiry command records exactly one system-attributed history row before Trip Home/review/application can treat elapsed proposals as pending.
- (decision) Trip Planning repair: the primary conversation pointer must always resolve to a same-owner linked live conversation after migration, and project-level fencing governs primary selection or replacement.
- (event) Trip Planning architecture review repairs applied
- (decision) Trip Planning repair follow-up: ordering scope is (trip_project_id, parent_item_id), and every backup references one same-project plan-item target; no separate decision-group aggregate is introduced in this tranche.
- (decision) Trip Home product decision: select pending expiring proposal, then pending proposal, then defined confirmed-item gap, then next dated planned/confirmed leg, then preparation; confirmed means owner confirmation or supplied constraint only, while lifecycle phases and on-trip today focus remain deferred.
- (constraint) The PRD requires auditable, actor/timestamped Trip Proposal history; operator-controlled auditable Facebook capture; provenance for knowledge automation; and AI usage records for authenticated requests. It does not require automated executors to be users.
- (direction) Fast-path architecture update for proposal-eliminate-fake-system-users-with-audit-actors.md; no additional solution-design deliverable requested.
- (decision) Adopt a first-class discriminated AuditActor/execution-actor contract: user actors carry a real users FK and immutable email snapshot; system actors carry a cataloged system ID and no users FK or person email. The union replaces authenticated-session-shaped worker identities at audit boundaries.
- (decision) Preserve deployed system identifiers system-knowledge-pipeline, system-trip-planning, and system-facebook-capture as immutable catalog IDs. Do not rewrite historical IDs to display-oriented aliases; catalog labels may evolve separately. system-youtube-capture remains seed-only unless preflight proves historical/runtime use, then it becomes a catalog ID.
- (decision) Separate user provenance from execution: requester/submitter/reviewer/owner fields remain real-user FKs; autonomous side effects use executor actor fields. AI usage stores nullable initiated-by user and required execution actor so user roster metrics exclude background/retry use while operations reports aggregate system execution.
- (decision) Migrate all user-or-system actor surfaces with one shared relational XOR shape, including audit_events and trip_plan_change_history. Enforce it after transactional backfill; every forward migration preflights all users-FK references and fails closed before reserved-user deletion.
- (decision) AD-31 review repair: system catalog includes AI orchestration and approved YouTube capture; legacy extraction and indexing share system-knowledge-pipeline. The required executor_system shape applies to automated knowledge/capture/usage artifacts while real-user semantic fields remain human-only. Facebook/YouTube discovered sources inherit originating human submitter and record capture only as executor.
- (decision) AD-31 review repair: audit owns the AuditActor union, catalog, session conversion, validation, and typed write helpers; direct writes to audit_events, trip_plan_change_history, and ai_usage_events are prohibited. ai_usage_events has exact initiated_by_user_id plus executor_system semantics.
- (event) Audit actor architecture update finalized
- (decision) AD-31 rollout correction: development database is reset daily and has no durable history requirement. Implement as one clean-break schema/API/migration/seed/test change, then reset and reseed. If durable production or customer data appears first, halt and redesign as an expand-migrate-contract rollout.
- (decision) AD-31 clean-break test correction: validate fresh migration and seed output rather than legacy backfill or guarded deletion behavior.
- (decision) AD-32 cutover: readable Facebook and YouTube capture versions atomically create exactly one canonical knowledge_ingestion_jobs row; knowledge:ingestion-worker is the only supported post-capture processing/publication worker. New admin actions, scripts, and scheduled services must not enqueue knowledge_extraction_jobs for a capture version.
- (decision) AD-32 legacy boundary: knowledge_extraction_jobs and knowledge:extraction-worker are historical compatibility only. They are disabled in normal production topology and may run only under an explicitly approved, time-bounded recovery/migration procedure after reconciliation with canonical ingestion jobs; they must not auto-approve cards from capture versions.
- (decision) AD-32 operations: source_capture_versions are immutable content-hashed capture identities with a current-source pointer and a one-to-one canonical ingestion-job relationship. Deployment supervises knowledge-ingestion and knowledge-indexing; manifests and runbooks must not advertise legacy extraction as a routine worker.
- (event) Unified capture ingestion architecture update recorded
- (decision) 2026-08-04: Knowledge lifecycle normalization replaces overlapping card states and business job stages with one lifecycle state, immutable candidate disposition, transactional transitionKnowledgeCard boundary, direct API/operator callers, Worker-only continuous processing, and forward-only single-writer rollout.
- (decision) 2026-08-04: Knowledge lifecycle normalization is a clean-break change while all current databases are disposable and reset; it removes legacy lifecycle representation without backfill, dual write, release matrix, or compatibility path, and must be redesigned as expand-migrate-contract if durable shared/customer data exists before shipping.
- (decision) 2026-08-04: Lifecycle trigger ownership is explicit: authenticated API commands synchronously invoke transitionKnowledgeCard for authorized operator decisions, while Worker exclusively owns continuous job claiming, ingestion, indexing, conflict processing, and scheduled sampling selection. Both use the same transactional boundary; API never claims jobs.
- (decision) 2026-08-04: Completed candidate disposition and reason are database-trigger immutable; job counters are idempotent observability projections. Active lifecycle requires eligible active supporting evidence, and source removal completes only after every dependent card is re-evaluated with no removed evidence traveler eligible.
- (decision) 2026-08-04: knowledge_sampling_obligations records one immutable obligation per needs_operator candidate, separate from actionable recommendations. Cohort containment persists exact membership and either creates fenced risk work for remediable pending cards or suppresses unsafe cards without successor work.
- (direction) Fast-path v6.2 update uses progressive disclosure: the Spine remains the authoritative invariant contract; a focused solution design and contracts, fixtures, and evaluation companions serve developers and coding agents.
- (constraint) Brownfield reality on 2026-08-11: TripAnswerContext is v1; trip_plan_items has free-text route endpoints and no canonical path reference; retrieval still persists a target count of three and uses it as a broad-query web trigger. V6.2 requires forward migration, shadow evidence, and explicit cutover rather than documentation that pretends the capability exists.
- (decision) Architecture authority is layered: ARCHITECTURE-SPINE.md owns paradigms, owners, mutation and safety invariants, and stable AD IDs; retrieval-trip-aware-solution-design.md owns the end-to-end projection; companion contract, fixture, and gate documents own executable detail. Companions cite AD IDs and never override them.
- (decision) Required planning needs use one versioned Retrieval-owned vocabulary and intent-to-requirement profile. Each execution materializes requirement keys before candidate generation; coverage is evaluated from exact rendered contributions as satisfied, missing, requires_verification, or requires_clarification. Card count never establishes sufficiency, and unrelated or wrong-leg evidence cannot satisfy a need.
- (decision) AI Orchestration selects one replayable planning mode from current_plan, explore_change, validate_proposal, or unscoped_answer after owner and URL-scope validation. Chat/Trips supplies an immutable exact Trip snapshot; current-turn intent is bounded and ephemeral; pending proposal context is exact and fenced. Retrieval consumes the pinned decision and does not reinterpret chat text as committed Trip authority.
- (decision) Chat/Trips extends transport leg state with nullable canonical origin, destination, selected path, and registry snapshot references. These references change only through owner-confirmed version-fenced Trip proposal operations. Existing labels migrate as unresolved with null canonical references; no inferred backfill gains route authority. Retrieval owns the immutable versioned route registry and validates referenced paths.
- (decision) Retrieval resolves each query leg to authoritative_selected, authoritative_complete, known_partial, ambiguous_paths, or no_path against an exact registry snapshot and active coverage assertion. Only exact selected or complete asserted paths grant hard negative authority; partial and ambiguous results remain soft, and no_path disables route-wide claims. Traveler projection exposes bounded useful guidance, limitation, and next action.
- (decision) Web fallback is planned per uncovered or freshness-sensitive requirement. Search captures immutable provider results; Retrieval creates immutable fact-scoped geography projections keyed by capture payload hash, registry snapshot, and resolver version, then records query-specific scope decisions. Unresolved or mismatched geography cannot become a factual premise or satisfy coverage.
- (decision) Feedback/Eval owns immutable datasets, cohorts, run results, and versioned numeric RetrievalGateProfile records. Critical authoritative cohorts use zero-tolerance safety constraints; standard cohorts use approved statistical guardrails. Release evidence pins corpus, Trip fixtures, runtime policy, retrieval mode, registry, and profile versions. Product Owner owns production cutover approval; module owners own safe execution and rollback.
- (decision) The broad-query fewer-than-three trigger is compatibility-only. It runs only in legacy or v6_shadow comparison, is subordinate to required-need coverage, and never marks a need satisfied. Retirement requires an approved vocabulary and Architecture contract, versioned broad-query and missing-need cohorts, a passing shadow evidence window, a recorded evaluation report, and Product Owner cutover approval; rollback changes the versioned read mode without destructive data rollback.
- (decision) Replay artifacts derived from a traveler message or Trip snapshot follow their owner deletion lifecycle. Chat deletion invalidates only its reconstructable execution payloads and cannot mutate unrelated Trip state; primary-conversation deletion must replace the pointer or delete the Trip; Trip deletion invalidates snapshots, canonical path references, retrieval runs, projections, and derived context while retaining only non-reconstructive audit metadata.
- (decision) V6.2 production retrieval baseline is scope-first deterministic eligibility and geographic/facet allowlisting followed by field-aware PostgreSQL full-text search and marginal required-need coverage selection. Existing token matching is legacy/shadow only after cutover. pg_trgm, embeddings, RRF, reranking, and AI grey-band adjudication remain experiment-gated and add no new source of truth or deployable service.
- (decision) Retrieval is sole writer for retrieval runs, requirement contributions, requirement outcomes, selection manifests, web query/scope decisions, and read-mode policy records; AI Orchestration is sole writer for prompt render manifests, terminal assistant messages, answer provenance, and usage finalization. A staged prepare/finalize protocol uses one run ID and idempotency fence; finalization seals the Retrieval run and persists terminal answer/provenance/usage in one PostgreSQL transaction, while a failed provider execution terminalizes without a completed assistant message.
- (decision) Retrieval owns a PostgreSQL-backed active read-policy record and compare-and-swap activateRetrievalReadPolicy command. The command requires the expected current policy version, exact passing gate report, Product Owner approval record, and rollback target; it atomically writes the new mode/policy and cutover audit. Deployment config may seed/cache but never write or override this authority.
- (decision) In v6_shadow, legacy remains the sole traveler-authoritative path. The v6 shadow performs no provider call, traveler mutation, response selection, prompt usage, or answer provenance write; it may store bounded evaluation telemetry only. Behavioral cutover and physical compatibility cleanup are separate gates. Before cleanup rollback may target legacy; after cleanup rollback targets a retained known-safe v6 policy version.
- (decision) An owner-confirmed Trip path referencing a retired or incompatible registry snapshot preserves its stored meaning for review/history but loses hard retrieval authority. Resolution becomes stale_selected_path with a typed reason and safe review/refresh outcome. Only an owner-confirmed set-leg-path proposal may bind the leg to a current validated registry snapshot; no automatic refresh is allowed.
- (decision) Chat/Trips deleteOwnedConversation and deleteOwnedTrip are the deletion coordinators. In one PostgreSQL transaction they fence the owner aggregate and invoke exported invalidators for Chat/Trips, Retrieval, Search, AI Orchestration, Feedback/Eval, and Usage reconstructable rows; success is returned only after all invalidations commit. Production evaluation membership is removed while aggregate non-content metrics may remain; user-message web captures are deleted or tombstoned and cannot be reused as knowledge.
- (decision) Requirement key identity is a deterministic content-addressed digest of the exact intent-profile version and canonical facet, importance, scope/leg, constraint, and freshness fields. The profile owns expansion cardinality, per-leg duplication, and duplicate coalescing; any generation change requires a new profile version. Retrieval creates immutable knowledge or web RequirementContribution records with exact source fact, eligibility/scope/freshness decisions, owner revisions, and permitted render variants; later stages consume only.
- (decision) Replayable web fallback includes an immutable WebQueryPlanManifest that binds each minimized query to requirement keys, allowed scope terms, excluded private-context classes, query builder and provider policy versions, and request digest. Each derived web fact pins its text digest and segmentation/extraction version. The persisted chain is capture to fact assertion/projection to requirement/leg scope decision to RequirementContribution to render manifest to provenance.
- (decision) Retrieval registry publishing has one writer command, publishRouteRegistryRelease, invoked through an authorized bounded operations path in the existing Worker runtime. It drafts and validates a code-reviewed reference manifest, builds required projections, and atomically compare-and-swap activates one immutable registry release plus coverage assertion set per read policy; partial failure leaves the previous active release and grants no authority.
- (decision) RetrievalGateProfile uses a closed mandatory metric contract with versioned denominators and explicit safety, quality, and operational limits; profile validation rejects any missing SC-8 through SC-12 or AC-28 through AC-33 criterion. A cutover evidence window contains one exact comparable dependency and code tuple; any relevant version change restarts the window rather than averaging different systems.
- (decision) Brownfield topology correction: the modular monolith is one workspace/domain/data architecture with four existing process/deployment units: traveler Next.js presentation, admin Next.js presentation, NestJS API, and Worker. Repository Docker targets are Railway-oriented for the current deployment convention; actual production provider/database evidence remains an operational gate rather than a Vercel architecture assumption.
- (decision) Amend the v6.2 lexical decision: scope-first field-aware lexical retrieval is binding, while PostgreSQL FTS using the exact provider/version and Vietnamese text-search configuration is conditional on a G0 simple-plus-unaccent deployability and quality spike. If the spike or critical recall/false-exclusion gates fail, v6_active keeps a deterministic indexed field-aware lexical implementation and FTS remains inactive; optional pg_trgm/vector/AI stages stay separately gated.
- (decision) AI Orchestration owns finalizeAiAnswer workflow coordination and answer provenance, not other aggregates. In one shared PostgreSQL transaction it invokes transaction-aware owner ports: Chat/Trips inserts the assistant message, Retrieval seals the run, Usage appends the usage event, and AI Orchestration writes prompt/provenance. The coordinator cannot import or directly write another owner's tables.
- (decision) Mandatory safety values in RetrievalGateProfile are literal invariants: hard-off-route, unrelated-need satisfaction, source-metadata leakage, web-scope premise misuse, recent-warning-as-live-authority, provider-failure unsafe recovery, hypothetical/pending-as-committed, private Trip leakage, silent required-gap omission, and critical hard-filter/cap false exclusion are zero; material provenance correctness is one. A complete profile with weaker values is invalid.
- (decision) Every v6_shadow traveler request creates one immutable paired retrieval execution with an authoritative legacy run and optional shadow run linked by execution ID and explicit roles. Only the authoritative run may select/persist the answer, prompt usage, provenance, or provider usage. Shadow persists a would-render evaluation manifest, never a prompt render manifest; comparison pins both run/policy/config/code identities and deletion invalidates the pair together.
- (decision) Read-policy CAS has discriminated transition reasons. Shadow, cutover, and cleanup require a current passing qualification report and Product Owner approval. Emergency rollback requires a failing rollback report or incident, authorized actor, expected-current CAS, and a target already recorded as runnable and previously qualified/approved; it requires no new passing report. The record stores target qualification and trigger report/incident IDs. The gate profile owns the minimum legacy rollback window, Feedback/Eval owns the cleanup report, Product approves cleanup, and Retrieval executes physical retirement only after COMP-06 passes.
- (event by codex) v6.2 retrieval and Trip-aware architecture update finalized after lint and three independent reviewer gates passed with zero Critical, High, or Medium findings
- (decision by user) Detailed planning uses a server-owned multi-turn clarification preflight: natural-language replies are extracted repeatedly, but deterministic server rules decide completeness and main answer generation waits while material required context remains unresolved
- (decision by user) Clarification profiles apply beyond itineraries to accommodation, food, and activities; context sufficiency is evaluated per requested deliverable and exact journey/day/leg/stop scope rather than as one global preference set
- (decision by user) Scoped preferences may vary across one Trip, such as higher accommodation quality at a destination and simple sleep-only stays on transit legs; narrower explicit values override compatible broader defaults and never leak to sibling scopes
- (decision by codex) Retrieval owns immutable planning-context profiles and pure completeness rules; AI Orchestration owns clarification sessions and preflight extraction coordination; Chat/Trips remains sole authority for applied Trip state and durable mutations
- (decision by codex) A clarification cycle requires a new traveler message before another extraction prompt; it is not an autonomous model loop. Partial replies merge by evidence-backed field and the next question asks only unresolved material fields until ready, declined, superseded, or safely bounded assumption mode
- (decision by codex) Reviewer repair: Chat/Trips owns persisted clarification sessions under AD-6; AI Orchestration coordinates synchronous plan/extract prompts and owner ports; Retrieval owns immutable profiles, scope/decomposition validation, and pure readiness evaluation
- (decision by codex) One active clarification session contains independently ready typed deliverable instances; immutable answer claims include only ready instances and main finalization rechecks session/content/profile/scope/assumption/Trip fences
- (decision by codex) Scoped context uses a versioned graph/comparator; strict ancestry or explicit profile precedence permits overrides, incomparable overlap is ambiguous, and profile-declared semantic groups let future concrete destination/transit/day instances inherit without global leakage
- (decision by codex) Profiled turns run bounded synchronous clarification_plan and clarification_extract prompt stages under the existing extraction model purpose and suppress the overlapping background context-extraction outbox event; no new runtime or config
- (decision by codex) Blocked clarification turns are replayable terminal AI Ask successes with atomic session/message/usage/command persistence and no Retrieval/web/main-answer artifacts; assumptions are immutable and must render explicit disclosure
- (decision by codex) Closure repair: Retrieval owns reusable context/plan policies and validates proposals, while Chat/Trips atomically owns traveler-instantiated scope graph revisions and sessions through initialize/evolve/reduce CAS commands idempotent by plan attempt
- (decision by codex) ClarificationPlanPolicy pins structural caps and rejects cycles, duplicates, orphan parents, and over-limit proposals; it is pinned through graph, session, claim, and evaluation evidence
- (decision by codex) Mixed parent session remains active while any instance collects/is ready/is claimed and completes only when all instances are completed or abandoned; traveler-derived graphs, validated plans, prompt attempts, and task digests follow owner deletion
- (event by codex) AD-39 scoped multi-turn clarification architecture finalized after lint and rubric, adversarial seam, and technology reality closure gates passed with zero Critical, High, or Medium findings
- (decision by user) When an unscoped conversation has sufficient durable planning context, the server exposes a persistent traveler CTA labeled Chuyển thành chuyến đi; not clicking is not a decline, and the CTA remains available while eligibility holds
- (decision by user) On conversion click, the server re-evaluates and transfers the latest eligible bounded context accumulated up to that command, not the stale context snapshot from when the CTA first appeared
- (decision by codex) Trip conversion persists no raw transcript or model reasoning; it atomically creates the Trip/primary conversation and one initial pending owner-review proposal from validated scoped context, and only proposal Apply makes it current Trip state
- (decision by codex) A stable CTA presentation resolves a current server-owned conversion opportunity/manifest revision; material context change refreshes or suspends eligibility, explicit dismissal creates the existing decline fence, and command revalidation prevents stale/ambiguous conversion
- (decision) AD-40 reuses and extends the existing acceptTripCreationRecommendation owner port and recommendation aggregate for current-manifest conversion; it adds no parallel conversion endpoint or authority
- (decision) AD-40 conversion projection is deterministic and versioned under Chat/Trips, requires at least one supported explicit scoped value, excludes assumption-only operations, and disables conversion while a newer traveler turn is unterminalized
- (decision) AD-40 closure: canonical conversation projection deterministically reduces eligible completed claims across sessions; opportunity transitions are CAS-closed; pending turns are server-projected visible-disabled; manifest stores exact typed payload with schema/serialization digest; success replay and deletion tombstones are explicit
- (decision) AD-40 profiled opportunity refresh runs synchronously after clarification/answer terminalization from canonical claims and never depends on the AD-39-suppressed context-extraction outbox or legacy chat_context; migration ownership stays within existing DB/domain/contracts/API/web modules
- (decision) AD-40 transition triggers are total: insufficient/ambiguous/reopened suspends and may recover on the same ID; deletion/ownership/scope loss or policy/schema withdrawal invalidates; only recoverable policy/schema withdrawal may later reproject into a new opportunity
- (event) AD-40 persistent latest-context chat-to-Trip conversion architecture finalized after lint and rubric, adversarial seam, and technology reality gates passed with zero Critical, High, or Medium findings
