# Admin presentation client. It calls the public HTTPS Nest API directly.
# Never add DATABASE_URL, Google credentials, bearer credentials, or private service secrets.
NEXT_PUBLIC_API_ORIGIN="https://api.xuyenviet.app"
