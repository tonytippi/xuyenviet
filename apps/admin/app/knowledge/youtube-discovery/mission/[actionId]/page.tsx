import { MissionDetail } from "./detail";
export default async function MissionActionPage({ params }: { params: Promise<{ actionId: string }> }) { const { actionId } = await params; if (!/^mission-[a-f0-9]{32}$/.test(actionId)) return <section><h1 className="text-2xl font-bold">Nhu cầu Discovery</h1><p className="mt-3 text-slate-700">Nhu cầu trong liên kết không khả dụng.</p></section>; return <MissionDetail actionId={actionId} />; }
