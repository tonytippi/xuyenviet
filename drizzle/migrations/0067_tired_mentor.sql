ALTER TABLE "knowledge_ingestion_jobs" ADD COLUMN "failed_candidate_count" integer DEFAULT 0 NOT NULL;--> statement-breakpoint
ALTER TABLE "knowledge_ingestion_jobs" ADD COLUMN "invalid_candidate_count" integer DEFAULT 0 NOT NULL;--> statement-breakpoint
CREATE UNIQUE INDEX "knowledge_ingestion_jobs_id_source_capture_idx" ON "knowledge_ingestion_jobs" USING btree ("id","source_id","capture_version_id");--> statement-breakpoint
ALTER TABLE "knowledge_ingestion_candidates" ADD CONSTRAINT "knowledge_ingestion_candidates_parent_capture_fk" FOREIGN KEY ("ingestion_job_id","source_id","capture_version_id") REFERENCES "public"."knowledge_ingestion_jobs"("id","source_id","capture_version_id") ON DELETE cascade ON UPDATE no action;--> statement-breakpoint
ALTER TABLE "knowledge_ingestion_candidates" ADD CONSTRAINT "knowledge_ingestion_candidates_outcome_reason_code_check" CHECK ("outcome_reason_code" is null or "outcome_reason_code" in ('candidate_terminalized', 'stale_or_deleted_capture', 'judge_model_unavailable', 'judge_model_not_independent', 'judge_provider_failed', 'relation_provider_failed', 'relation_ambiguous', 'relation_invalid', 'stale_relation_target', 'attach_condition_mismatch', 'conflict_condition_mismatch', 'retry_exhausted'));--> statement-breakpoint
ALTER TABLE "knowledge_ingestion_jobs" DROP CONSTRAINT "knowledge_ingestion_jobs_candidate_counts_check";--> statement-breakpoint
ALTER TABLE "knowledge_ingestion_jobs" ADD CONSTRAINT "knowledge_ingestion_jobs_candidate_counts_check" CHECK ("discovered_candidate_count" >= 0 and "terminal_candidate_count" >= 0 and "terminal_candidate_count" <= "discovered_candidate_count" and "published_candidate_count" >= 0 and "suppressed_candidate_count" >= 0 and "review_recommended_candidate_count" >= 0 and "verify_first_candidate_count" >= 0 and "failed_candidate_count" >= 0 and "invalid_candidate_count" >= 0 and "published_candidate_count" + "suppressed_candidate_count" + "review_recommended_candidate_count" + "verify_first_candidate_count" + "failed_candidate_count" = "terminal_candidate_count");
